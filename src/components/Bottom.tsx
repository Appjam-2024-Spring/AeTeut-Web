import styles from "../styles/bottom.module.css";

export default function Bottom() {
  return (
    <div className={styles["container-button"]}>
      <button className={styles["button"]}>앱에서 확인하기</button>
    </div>
  );
}
